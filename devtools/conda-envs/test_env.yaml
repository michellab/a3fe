name: test
channels:

  - conda-forge
  - "openbiosim/label/dev"
  - "openbiosim"

  - defaults
dependencies:
    # Base depends
  - arviz
  - python
  - pip
  - numpy
  - matplotlib
  - scipy
  - ipython
  - pymbar<4
  - ambertools
  - biosimspace
  - numpydoc

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

