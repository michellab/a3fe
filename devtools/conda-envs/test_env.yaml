name: test
channels:

  - conda-forge
  - "openbiosim"
  - "openbiosim/label/dev"

  - defaults
dependencies:
    # Base depends
  - arviz
  - python
  - pip
  - numpy
  - matplotlib
  - scipy
  - ipython
  - pymbar<4
  - ambertools
  - biosimspace
  - numpydoc
  - importlib-resources

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

